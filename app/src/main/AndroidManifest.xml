<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.autoe.autoecustomer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
        <activity android:name=".BookingActivity"></activity>
        <activity android:name=".ReadMoreActivityFromSelectYourPackage" />
        <activity android:name=".SelectYourPackage" />
        <activity android:name=".AddACar" />
        <activity android:name=".SelectACar" />
        <activity android:name=".AddALocation" />
        <activity android:name=".MakeABooking" />
        <activity
            android:name=".HelpActivity"
            android:label="Help" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PhoneAuth" />
        <activity android:name=".PhoneVerification" />
        <activity android:name=".AccountActivity" />
    </application>

</manifest>